apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-auth
data:
  admin_emails: {{ .Values.db.admin_emails | b64enc | quote  }}
  database_url: {{ .Values.db.database_url | b64enc | quote  }}
  secret_key_base: {{ .Values.db.secret_key_base | b64enc | quote  }}
  ses_password: {{ .Values.aws.ses_password | b64enc | quote  }}
  ses_username: {{ .Values.slack.ses_username | b64enc | quote  }}
